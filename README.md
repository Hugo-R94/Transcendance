## Transfer minichat

pour transferer le minichat et le isRead + isTyping suivre ces etapes : 


# pour le front 

- copier coller  le contenu de chatmenu.tsx dans le chat menu cible.
- copier coller le contenu de chatwindow.tsx dans le chat window cible
- copier coller le contenu de chatwindowcontainer.tsx dans le chat window container cible
- transferer les fichiers MinimizedHeader.tsx  MessageBubble.tsx chatWindowHeader.tsx 
- supprimer le minichat dans app.tsx

# pour le back

- copier colle le contenu entier de apihandler/chat/ fichier par fichier
inte